info:
  name: Get All Items (Direct)
  type: http
  seq: 4

http:
  method: GET
  url: "{{direct_base_url}}:{{item_port}}/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
