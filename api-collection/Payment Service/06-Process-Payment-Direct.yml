info:
  name: Process Payment (Direct)
  type: http
  seq: 6

http:
  method: POST
  url: "{{direct_base_url}}:{{payment_port}}/payments/process"
  auth: inherit
  headers:
    - key: Content-Type
      value: application/json
  body:
    mode: raw
    raw: |-
      {
        "amount": 299.99,
        "orderId": 2,
        "cardNumber": "4222222222222222",
        "expiryDate": "06/27",
        "cvv": "456"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
