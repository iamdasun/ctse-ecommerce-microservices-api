info:
  name: Get All Payments (Direct)
  type: http
  seq: 4

http:
  method: GET
  url: "{{direct_base_url}}:{{payment_port}}/payments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
