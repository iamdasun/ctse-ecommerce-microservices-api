info:
  name: Get Payment by ID (via Kong)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/payments/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
