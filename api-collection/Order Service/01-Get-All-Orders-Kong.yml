info:
  name: Get All Orders (via Kong)
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
