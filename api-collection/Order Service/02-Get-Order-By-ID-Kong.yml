info:
  name: Get Order by ID (via Kong)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/orders/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
