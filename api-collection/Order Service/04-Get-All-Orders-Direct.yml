info:
  name: Get All Orders (Direct)
  type: http
  seq: 4

http:
  method: GET
  url: "{{direct_base_url}}:{{order_port}}/orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
