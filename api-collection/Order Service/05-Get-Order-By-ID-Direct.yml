info:
  name: Get Order by ID (Direct)
  type: http
  seq: 5

http:
  method: GET
  url: "{{direct_base_url}}:{{order_port}}/orders/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
