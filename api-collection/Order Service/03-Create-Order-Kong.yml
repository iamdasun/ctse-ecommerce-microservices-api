info:
  name: Create Order (via Kong)
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/orders"
  auth: inherit
  headers:
    - key: Content-Type
      value: application/json
  body:
    mode: raw
    raw: |-
      {
        "itemId": 1,
        "quantity": 2,
        "userId": "user123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
